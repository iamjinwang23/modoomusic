{
  "name": "mono",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "web": "npm run dev -w web",
    "mobile": "npm run start -w mobile"
  },
  "devDependencies": {
    "node-addon-api": "^8.9.0",
    "node-gyp": "^12.4.0",
    "turbo": "^2.3.0"
  }
}
